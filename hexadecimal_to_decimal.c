#include<stdio.h>
int main()
{
    int number;
    // Inputting a Hexadecimal number from user
    printf( "Enter any Hexadecimal number :");
    scanf("%x",&number);
  // Converting Hexadecimal to Decimal
    printf(" Decimal number= %d\n", number);
    return 0;
}

