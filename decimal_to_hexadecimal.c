#include<stdio.h>
int main()
{
    int number;
    // Inputting a decimal number from user
    printf( "Enter any decimal number :");
    scanf("%d",&number);
   // Printing the equivalent Hexadecimal number using %x specifier
    printf(" Hexadecimal number= %x\n", number);
    return 0;
}

